# initialize playwright project - out of the box
- npm init playwright@latest
 
# initialize playwright project - from scratch
- npm init -y
- npx i -D @playwright/test
- npx playwright install

# create the playwright.config.ts
